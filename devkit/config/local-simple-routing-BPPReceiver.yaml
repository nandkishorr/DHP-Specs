routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "https://develop-api.ohc.network/api/v1/beckn/bpp/webhook"
    endpoints:
      - select
      - init
      - confirm
      - status
      - cancel
      - support
      - track
      - update
      - rate
